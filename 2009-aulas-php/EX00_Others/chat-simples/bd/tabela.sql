CREATE TABLE chat (
       id                   INTEGER NOT NULL,
       de                   VARCHAR(50) NULL,
       mensagem             TEXT NULL,
       quando               TIMESTAMP NULL,
       PRIMARY KEY (id)
);


